apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ci-cd-demo
spec:
  rules:
    - host: ci-cd-demo.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ci-cd-demo
                port:
                  number: 80
